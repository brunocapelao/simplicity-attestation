fn main() {
    // Admin public key
    let pk: u256 = 0xbcc13efe121b35c7270c41ce63dab7688c0326c58c5f5e1d1af317c1503cad45;
    let msg: u256 = jet::sig_all_hash();
    // Dummy signature (will be replaced with real one)
    let sig: [u8; 64] = witness("sig");
    jet::bip_0340_verify((pk, msg), sig)
}
